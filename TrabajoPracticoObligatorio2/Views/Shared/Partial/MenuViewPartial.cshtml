@{
    ViewBag.Title = "MenuViewPartial";
}

<section>
    <div class="container">
        <div class="menu">
            @* <button>@Html.ActionLink("Inicio", "Index", "Home")</button> *@
            @* <button>@Html.ActionLink("Markets", "Market", "Market")</button> *@
            @* <button>@Html.ActionLink("Contact", "Contact", "Contact")</button> *@
            @* *@
            <a href="@Url.Action("Index", "Home")"><button>Home</button></a>
            <a href="@Url.Action("Market", "Market")"><button>Markets</button></a>
            <a href="@Url.Action("Contact", "Contact")"><button>Contact</button></a>
            
            
            @* <a href="contact.html"><button>Contact</button></a> *@
        </div>
    </div>

</section>